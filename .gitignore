# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# PyInstaller build output
build/
dist/
dist_final/
*.spec

# Packaged app
*.zip

# Local data (large) — drop your own captures here, never committed.
# Ignore the CONTENTS (not the dirs) so each folder's .gitignore + README stay tracked.
recordings/*
datasets/*
*.png
*.bmp
*.pcapng
*.csv

# ...but the app's own bundled UI assets (icon) ARE tracked.
!src/winwinghaptics/ui/assets/**/*.png

# App runtime artifacts
crash_log.txt
hudtest_result.txt
boot_out.txt
winwing_haptics.json
*.calib.json
calib.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
tools/_crops/
tests/.cache/
tests/pinned_calib/
tests/digit_dataset/
tests/digit_model.npz
