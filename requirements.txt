# Runtime dependencies for WinwingHaptics.
#
# tkinter ships with the standard CPython Windows installer (no pip package).
numpy>=1.24
winsdk>=1.0.0b10

# Build-time only (packaging the standalone app):
#   pip install pyinstaller
